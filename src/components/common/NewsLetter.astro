<section id="news-letter">
    <div class="wrapper">
        <div class="news-letter-container">
            <div class="news-letter-container__left">
                <div class="news-letter-container__left__head">
                    <h3 class="section-sub-title">Subscribe to my <span>Newsletters</span></h3>
                    <p>
                        Lorem ipsum dolor sit amet consectetur adipisicing elit.
                        Tenetur vero esse non molestias eos excepturi, inventore atque cupiditate.
                    </p>
                </div>
            </div>

            <div class="news-letter-container__right">
                <form action="">
                    <div class="news-letter-container__right__input-container">
                        <input type="text" placeholder="Work Email" />
                        <button type="submit">Subscribe</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</section>

<style lang="scss">
    .news-letter-container {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1.5rem;

        @media all and (max-width: 640px) {
            flex-direction: column;
        }
        .news-letter-container__left {
            width: 58.333333%;
            @media all and (max-width: 640px) {
                width: auto;
            }
            .news-letter-container__left__head {
                h3 {
                    span {
                        color: transparent;
                        background: var(--text-gradient);
                        -webkit-background-clip: text;
                        background-clip: text;
                    }
                }
                p {
                    margin-top: .75rem;
                    color: var(--gray);
                }
            }
        }

        .news-letter-container__right {
            widows: 41.666667%;
            @media all and (max-width: 640px) {
                width: 100%;
            }
            form {
                .news-letter-container__right__input-container {
                    width: 100%;
                    background-color: var(--secondary);
                    border-radius: 9999px;
                    display: flex;
                    padding: .5rem 1rem;

                    border: 2px solid var(--secondary);
                    &:hover {
                        border-color: var(--tertiary);
                    }
                    &:focus {
                        border-color: var(--tertiary);
                    }
                    input {
                        appearance: none;
                        display: block;
                        outline: none;
                        border: none;
                        flex: 1;
                        background-color: var(--secondary);
                        width: 100%;
                        font-size: 100%;
                        color: inherit;
                    }

                    button {
                        outline: none;
                        border: none;
                        font-weight: 500;
                        font-size: .875rem;
                        line-height: 1.25rem;
                        padding: .25rem .75rem;
                        border-radius: 9999px;
                        background: var(--text-gradient);
                        flex-shrink: 0;
                        margin-left: .5rem;
                        color: inherit;
                    }
                }
            }
        }
    }
</style>

